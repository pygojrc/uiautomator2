# 2026-08-14 setup-matt-pocock-skills

- 更新仓库 `origin` 为 `git@github.com:pygojrc/uiautomator2.git`，并完成 fetch/push 地址核对。
- 按确认配置 GitHub Issues、默认 triage labels 和 single-context domain docs。
- 写入 `Claude.md` 的 Agent skills 说明，以及 `docs/agents/` 下的三个配置文件。
- 根据项目约定，将 Agent skills 配置移至新建的 `AGENTS.md`，恢复 `Claude.md` 原有内容。
